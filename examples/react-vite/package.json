{
  "name": "react-vite-example",
  "version": "1.0.0",
  "private": true,
  "description": "Vite + React + TypeScript example that verifies the local @jenbuska/yielded package works in a browser context.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "install:browsers": "playwright install --with-deps chromium firefox webkit",
    "test": "npm run build && npm run install:browsers && playwright test"
  },
  "dependencies": {
    "@jenbuska/yielded": "file:../../",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^5.1.4",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  }
}
